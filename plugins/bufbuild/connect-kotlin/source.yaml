source:
  # Users are encouraged to use the https://buf.build/connectrpc/kotlin plugin which supersedes
  # https://buf.build/bufbuild/connect-kotlin.
  disabled: true 
  github:
    owner: bufbuild
    repository: connect-kotlin
