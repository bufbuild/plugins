name: Fetch latest versions

on:
  pull_request:
  schedule:
    - cron: "0 0 * * *" # Run once a day
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write

jobs:
  fetch-versions:
    runs-on: ubuntu-22.04
    steps:
      - name: Generate token
        id: generate_token
        uses: tibdex/github-app-token@v1
        with:
          # ID from: https://github.com/organizations/bufbuild/settings/apps/app-token-exchange-remote-plugins
          app_id: 249762
          private_key: ${{ secrets.TOKEN_EXCHANGE_GH_APP_PRIVATE_KEY }}
          repository: ${{ github.repository }}
          permissions: >-
            {"contents": "write", "pull_requests": "write"}
      - name: Checkout repository code
        uses: actions/checkout@v3
        with:
          token: ${{ steps.generate_token.outputs.token }}
      - name: Create Pull Request
        id: cpr
        uses: peter-evans/create-pull-request@v4
        with:
          token: ${{ steps.generate_token.outputs.github_token }}
          commit-message: Update report
          committer: GitHub <noreply@github.com>
          author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
          signoff: false
          branch: example-patches
          delete-branch: true
          title: '[Example] Update report'
          body: |
            Update report
            - Updated with *today's* date
            - Auto-generated by [create-pull-request][1]
            
            [1]: https://github.com/peter-evans/create-pull-request
          labels: |
            report
            automated pr
          assignees: elliotmjackson
          reviewers: elliotmjackson
          milestone: 1
          draft: false
          #      - name: Fetch all versions
          #        env:
          #          GITHUB_TOKEN: ${{ steps.generate_token.outputs.github_token }}
          #        run: |
          #          go run ./cmd/fetcher .
          #      - name: Create PR
          #        uses: peter-evans/create-pull-request@ad43dccb4d726ca8514126628bec209b8354b6dd
          #        with:
          #          add-paths: .
          #          commit-message: "detected new plugin versions"
          #          branch: fetch-versions
          #          delete-branch: true
          #          title: "Found new plugin versions"
          #          body: "New plugin versions found. Please review."
          #          assignees: mfridman, pkwarren
          #          token: ${{ steps.generate_token.outputs.github_token }}
          
